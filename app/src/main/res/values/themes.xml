<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Usamos un tema base estándar que no requiera dependencias XML externas de Material 3 si no están bien enlazadas -->
    <style name="Theme.SikRepMus" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
